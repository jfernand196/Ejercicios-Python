# Solicitar el valor del radio de un circulo para clacular el area
from math import pi

r = float(input('escriba el radio del circulo: '))
area = pi*r**2

print('El area del circulo es {}'.format(area))

